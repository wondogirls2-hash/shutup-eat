# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note: this project intentionally targets Expo SDK 54 (not the latest SDK) because the
Expo Go app distributed on the App Store/Play Store lags behind the newest SDK release.
Do not upgrade the `expo` package without checking Expo Go store compatibility first.
